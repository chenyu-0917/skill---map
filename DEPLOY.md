# 🚀 GitHub Pages 部署指南

## 📋 部署步驟

### 1️⃣ 建立 GitHub Repository

1. 登入 [GitHub](https://github.com)
2. 點擊右上角的 `+` → `New repository`
3. 填寫專案資訊:
   - **Repository name**: `skill-map` (或你喜歡的名稱)
   - **Description**: "個人技術技能地圖 - 互動式視覺化工具"
   - **Public** (公開) 或 **Private** (私有,但 Pages 需付費)
   - ✅ 勾選 "Add a README file" (如果你要用這個 README)
4. 點擊 `Create repository`

### 2️⃣ 上傳檔案到 GitHub

#### 方法一:使用 Git 指令 (推薦)

```bash
# 1. 初始化 Git (在專案資料夾中)
git init

# 2. 將所有檔案加入追蹤
git add .

# 3. 建立第一次 commit
git commit -m "🎉 Initial commit: 建立技能地圖專案"

# 4. 連結到你的 GitHub repository
git remote add origin https://github.com/你的使用者名稱/skill-map.git

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

#### 方法二:使用 GitHub 網頁介面

1. 在 GitHub repository 頁面點擊 `Add file` → `Upload files`
2. 拖曳以下檔案上傳:
   - `skill-map.html`
   - `README.md`
   - `.gitignore`
3. 填寫 commit 訊息,例如:"新增技能地圖檔案"
4. 點擊 `Commit changes`

### 3️⃣ 啟用 GitHub Pages

1. 進入你的 repository 頁面
2. 點擊 `Settings` (設定)
3. 在左側選單找到 `Pages`
4. 在 **Source** 區域:
   - Branch: 選擇 `main`
   - Folder: 選擇 `/ (root)`
5. 點擊 `Save`
6. 等待約 1-2 分鐘,頁面會顯示你的網站網址

### 4️⃣ 設定首頁檔案

為了讓網站可以直接顯示,有兩種方式:

#### 選項 A:重新命名檔案 (推薦)
```bash
# 將 skill-map.html 重新命名為 index.html
git mv skill-map.html index.html
git commit -m "重新命名為 index.html"
git push
```

#### 選項 B:保持原檔名
訪問時使用完整路徑:
```
https://你的使用者名稱.github.io/skill-map/skill-map.html
```

### 5️⃣ 訪問你的技能地圖 🎉

部署完成後,你可以透過以下網址訪問:

```
https://你的使用者名稱.github.io/skill-map/
```

例如,如果你的使用者名稱是 `john-doe`:
```
https://john-doe.github.io/skill-map/
```

## 🔄 更新技能地圖

當你想更新技能資料時:

```bash
# 1. 編輯 skill-map.html (或 index.html)

# 2. 提交變更
git add .
git commit -m "更新技能資料"

# 3. 推送到 GitHub
git push

# 4. 等待約 1-2 分鐘,GitHub Pages 會自動更新
```

## 💡 進階設定

### 自訂網域

如果你有自己的網域:

1. 在 repository 的 `Settings` → `Pages` → `Custom domain`
2. 輸入你的網域,例如:`skills.example.com`
3. 在你的網域 DNS 設定中新增 CNAME 記錄

### 自動化部署

可以使用 GitHub Actions 來自動化部署流程(進階功能)。

## ❓ 常見問題

**Q: 為什麼我的網站顯示 404?**
- 確認檔案名稱是 `index.html` 或使用完整路徑訪問
- 等待 1-2 分鐘讓 GitHub Pages 完成部署

**Q: 可以設定為私有嗎?**
- 免費版的 GitHub Pages 只支援公開 repository
- 私有 repository 需要 GitHub Pro

**Q: 更新後沒有變化?**
- 清除瀏覽器快取 (Ctrl+F5 或 Cmd+Shift+R)
- 等待 GitHub Pages 重新部署(約 1-2 分鐘)

## 🎯 下一步

- ⭐ 為 repository 新增星星
- 📝 自訂你的技能資料
- 🎨 調整配色和樣式
- 📱 新增專案截圖到 README
- 🔗 分享你的技能地圖連結!

---

**祝你部署順利!** 如有問題歡迎開 Issue 討論 💪
